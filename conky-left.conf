conky.config = {
    -- Window
    own_window            = true,
    own_window_class      = 'Conky',
    own_window_type       = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value  = 255,
    own_window_hints      = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    -- Position: top-right, offset to leave room for the clock panel (130px + 20px gap + 60px edge = 210)
    alignment       = 'top_right',
    gap_x           = 210,
    gap_y           = 60,

    minimum_width   = 270,
    minimum_height  = 135,
    maximum_width   = 270,

    update_interval = 1,
    double_buffer   = true,
    no_buffers      = true,
    text_buffer_size = 2048,

    use_xft   = true,
    font      = 'Inter:size=10',
    xftalpha  = 1.0,
    draw_shades = false,
    default_color = 'ffffff',

    lua_load          = '~/.config/conky/widget-left.lua',
    lua_draw_hook_pre = 'draw_bg',
}

conky.text = [[
${voffset 15}${alignc}${font DotGothic16:size=15}${color cccccc}${time %d-%b, %A}${font}${color}
${voffset 35}${alignc}${font Special Elite:size=13}${color cccccc}Every day is a summit push${font}${color}
]]
