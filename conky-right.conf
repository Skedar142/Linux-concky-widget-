conky.config = {
    -- Window
    own_window            = true,
    own_window_class      = 'Conky',
    own_window_type       = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value  = 255,
    own_window_hints      = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    -- Position: top-right, 60px from edge
    alignment       = 'top_right',
    gap_x           = 60,
    gap_y           = 60,

    minimum_width   = 130,
    minimum_height  = 145,
    maximum_width   = 130,

    update_interval = 1,
    double_buffer   = true,
    no_buffers      = true,
    text_buffer_size = 2048,

    use_xft     = true,
    font        = 'NDOT 45:size=10',
    xftalpha    = 1.0,
    draw_shades = false,
    default_color = 'ff9900',

    lua_load          = '~/.config/conky/widget-right.lua',
    lua_draw_hook_pre = 'draw_bg',
}

-- Hours stacked over minutes, both centered in the box
conky.text = [[
${voffset 0}${alignc}${font NDOT 45:size=42}${color ff9900}${time %H}${font}${color}
${voffset 0}${alignc}${font NDOT 45:size=42}${color ff9900}${time %M}${font}${color}
]]
